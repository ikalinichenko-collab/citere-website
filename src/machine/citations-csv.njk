---
permalink: /citations.csv
eleventyExcludeFromCollections: true
---
{#- Domain × bot citation counts from the published Sources Registry. -#}
domain,network,category,chatbot,cited_count,critical_count,url
{% for s in publishedSources %}{% for bot, n in s.byBot %}{{ s.domain | csvCell }},{{ s.network | csvCell }},{{ s.category | csvCell }},{{ bot | csvCell }},{{ n }},{{ s.criticalCount }},{{ (site.url + s.url) | csvCell }}
{% endfor %}{% endfor %}
